# Rust Exercises Project

This project collects the Rust problems.

## Included topics (23 total)

1. Storing data on the heap with `Box<T>`
2. Customizing `*` with `Deref`
3. Multiple owners with `Rc<T>`
4. Interior mutability with `RefCell<T>`
5. Dynamic dispatch with trait objects
6. Building a processing pipeline with trait objects
7. The newtype wrapper trick
8. Associated types vs generics
9. Implementing `Add` and `Display`
10. Functions as values
11. Returning closures with `Box<dyn Fn>`
12. More powerful matching
13. Parsing with slice patterns
14. Escaping the borrow checker with raw pointers
15. The safe wrapper pattern
16. Code generation with macros
17. Pattern-based code generation with macros
18. Handling multiple arguments with macro repetition
19. Fearless concurrency with threads
20. Shared state with `Arc<Mutex<T>>`
21. A channel pipeline with worker threads
22. A Judge0-friendly `Arc<Mutex<T>>` counter variant
23. An exact-`n`-threads `Arc<Mutex<T>>` counter variant

## Run

```bash
cargo run
```

## Check

```bash
cargo check
```
